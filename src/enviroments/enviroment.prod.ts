/* The code `export const environment = { production: true };` is defining a constant variable named
`environment` and exporting it. The `environment` variable is an object with a single property
`production` set to `true`. This code is typically used in a TypeScript project to define the
environment configuration for a production build. */
export const environment = {
  production: true,
};
